1. 安装pip

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

python get-pip.py

2. 安装imutils

sudo pip install imutils

3. 安装pygame

sudo apt-get install python-pygame

4. 两个model文件必须要有

5. 先开zed 

roslaunch zed_wrapper zed.launch publish_tf:=false

6. python real_time_object_detection_6.30.py
